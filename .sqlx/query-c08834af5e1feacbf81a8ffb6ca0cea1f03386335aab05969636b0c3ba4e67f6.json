{
  "db_name": "PostgreSQL",
  "query": "\n        SELECT user_id, username, COALESCE(total_points, 0)::INT as \"total_points!\"\n        FROM (\n            SELECT\n                sub1.id AS user_id,\n                sub1.USERNAME,\n                SUM(COALESCE(picks.POINTS, 0)) AS TOTAL_POINTS\n            FROM (\n                SELECT users.id, users.username\n                FROM users\n                JOIN subscriptions on users.id = subscriptions.user_id\n                WHERE book_id = $1\n            ) as sub1\n            LEFT JOIN picks on sub1.id = picks.user_id\n            WHERE\n                picks.CHAPTER_ID = $2 OR picks.CHAPTER_ID IS NULL\n            GROUP BY\n                sub1.ID,\n                sub1.USERNAME\n        ) AS sub2\n        ORDER BY total_points DESC, username\n        ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "user_id",
        "type_info": "Int4"
      },
      {
        "ordinal": 1,
        "name": "username",
        "type_info": "Text"
      },
      {
        "ordinal": 2,
        "name": "total_points!",
        "type_info": "Int4"
      }
    ],
    "parameters": {
      "Left": [
        "Int4",
        "Int4"
      ]
    },
    "nullable": [
      false,
      false,
      null
    ]
  },
  "hash": "c08834af5e1feacbf81a8ffb6ca0cea1f03386335aab05969636b0c3ba4e67f6"
}
